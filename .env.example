# Optional: full API origin in production when the SPA is not served from the API host
# VITE_API_BASE_URL=https://api.example.com
# Must match backend ADMIN_API_KEY (sent as x-admin-key on admin save/reset)
VITE_ADMIN_API_KEY=change-me-in-production
